\section{Experiments}
\label{sec:experiments}

\subsection{Setup}
All experiments run on a single consumer GPU. Synthetic sequences are
generated by our controllable thermal scene model (drifting correlated
background, moving hot/cold objects with soft thermal edges, microbolometer
low-pass, PRNU, NETD noise, 14-bit quantization, AGC drift/jumps). Real
sources are HIT-UAV sample videos~\cite{suo2023hituav} (\SI{7.5}{\hertz},
$640{\times}512$) and the eight continuous sequences of the FLIR ADAS v2
video test set~\cite{fliradas} ($640{\times}512$, 3{,}145 frames total,
COCO labels).

\subsection{E1: interpolation audit}
\label{sec:e1}
We hold out every $k$-th frame from real thermal video, reconstruct it from
the remaining frames, and measure PSNR/SSIM for linear, cubic, and
flow-guided interpolation. Table~\ref{tab:e1} shows that
\emph{linear blending matches or beats flow-guided warping} on all five
HIT-UAV videos at $4\times$ upsampling (mean 29.77 vs 29.26 dB). Thermal
imagery is texture-poor, AGC-corrupted, and at \SI{7.5}{\hertz} exhibits large
non-rigid platform motion, violating the brightness-constancy assumption
that flow-guided (and learned, visible-light-trained) interpolators rely
on. We therefore default to linear interpolation, and flag the failure of
visible-light interpolation practice to transfer to thermal as an E1
finding.

\begin{table}[t]
\centering
\caption{Held-out frame reconstruction on HIT-UAV thermal video (PSNR dB, $4\times$). Linear blending is best on all videos; flow guidance does not transfer to the thermal domain.}
\label{tab:e1}
\begin{tabular}{lccc}
\toprule
video & linear & cubic & flow \\
\midrule
120m-30-3 & \textbf{27.56} & 27.37 & 26.81 \\
130m-60-3 & \textbf{31.07} & 30.77 & 30.75 \\
60m-30-1 & \textbf{36.21} & 35.98 & 35.27 \\
60m-40-3 & \textbf{26.97} & 26.65 & 26.89 \\
70m-90-1 & \textbf{27.04} & 26.86 & 26.57 \\
\midrule
mean & \textbf{29.77} & 29.53 & 29.26 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{E2: conversion and AGC de-flicker}
\label{sec:e2}
We convert all real sources and measure event statistics with and without
de-flickering (Fig.~\ref{fig:e2}). On synthetic scenes with known ground
truth, pivot-centred AGC correction reduces the static-scene spurious
event count to the \emph{exact} radiometric floor, whereas naive
two-parameter affine correction increases the event rate by an order of
magnitude by unpinning bulk pixels. On real HIT-UAV video the de-flicker
leaves the mean event rate largely unchanged on stable sequences but
suppresses AGC-induced bursts on re-ranging segments
(Fig.~\ref{fig:agc}), consistent with its gating behaviour.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/e2_rates.pdf}
\caption{Mean event rate with and without AGC de-flickering on the five
HIT-UAV videos.}
\label{fig:e2}
\end{figure}

\subsection{E3: fidelity and parameter sensitivity}
\label{sec:fidelity}
\label{sec:calibration}
We sweep simulator mode $\times$ threshold $\times$ shot noise $\times$
bolometer low-pass on fixed ground-truth sequences and report event-rate
statistics, polarity balance, spatial sparsity, and Edge--Event Alignment
(EEA) as a function of parameters. We further calibrate the
threshold$\leftrightarrow$NETD mapping by measuring event yield inside
known object boxes as object temperature contrast is swept
(Fig.~\ref{fig:calibration}); the measured minimum detectable contrast
matches the theoretical value to within experimental error (threshold 0.2
$\rightarrow \approx 4$ temperature units, in line with $\Delta T_{\min} = \theta \, I/(dI/dT)$ at mid display range).

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/calibration.pdf}
\caption{In-object event fraction as a function of object temperature
contrast for three simulator thresholds. The contrast at which the yield
rises from the noise floor defines the minimum detectable contrast and
calibrates the threshold$\leftrightarrow$NETD mapping.}
\label{fig:calibration}
\end{figure}

\subsection{E4: detection benchmark}
\label{sec:e4}
We train a shared CenterNet-style detector~\cite{zhou2019centernet} on
three input channels---thermal frame, event voxel grid, and early
fusion---plus a recurrent (ConvLSTM) event variant, isolating the input
representation as the only varied factor. An external YOLOv8n anchor is
trained on the FLIR thermal frame train split. We report COCO mAP and
mAP@50 overall and per slice (small/large, slow/fast, low/high contrast,
day/night, AGC-corrupted) on both STED and FLIR-STED.
\begin{table}[t]
\centering
\caption{Detection results (COCO mAP / mAP@50) on FLIR-STED and STED,
shared CenterNet detector across channels. Best per dataset in bold.}
\label{tab:e4}
\begin{tabular}{lcccc}
\toprule
 & \multicolumn{2}{c}{FLIR-STED} & \multicolumn{2}{c}{STED} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5}
channel & mAP & mAP@50 & mAP & mAP@50 \\
\midrule
thermal frame & --- & --- & --- & --- \\
event (voxel) & --- & --- & --- & --- \\
early fusion & --- & --- & --- & --- \\
event recurrent & --- & --- & --- & --- \\
\midrule
YOLOv8n frame (anchor) & --- & --- & --- & --- \\
\bottomrule
\end{tabular}
\end{table}

